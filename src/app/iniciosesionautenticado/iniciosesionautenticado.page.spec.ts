import { ComponentFixture, TestBed } from '@angular/core/testing';
import { IniciosesionautenticadoPage } from './iniciosesionautenticado.page';

describe('IniciosesionautenticadoPage', () => {
  let component: IniciosesionautenticadoPage;
  let fixture: ComponentFixture<IniciosesionautenticadoPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(IniciosesionautenticadoPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
