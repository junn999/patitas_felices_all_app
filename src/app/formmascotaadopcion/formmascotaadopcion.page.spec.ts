import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FormmascotaadopcionPage } from './formmascotaadopcion.page';

describe('FormmascotaadopcionPage', () => {
  let component: FormmascotaadopcionPage;
  let fixture: ComponentFixture<FormmascotaadopcionPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FormmascotaadopcionPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
