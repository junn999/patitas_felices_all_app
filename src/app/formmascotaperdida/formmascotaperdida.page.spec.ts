import { ComponentFixture, TestBed } from '@angular/core/testing';
import { FormmascotaperdidaPage } from './formmascotaperdida.page';

describe('FormmascotaperdidaPage', () => {
  let component: FormmascotaperdidaPage;
  let fixture: ComponentFixture<FormmascotaperdidaPage>;

  beforeEach(() => {
    fixture = TestBed.createComponent(FormmascotaperdidaPage);
    component = fixture.componentInstance;
    fixture.detectChanges();
  });

  it('should create', () => {
    expect(component).toBeTruthy();
  });
});
